const state = () => ({
  isMobileSize: undefined
});

const mutations = {};

const getters = {};

const actions = {
  init() {}
};

export default {
  state,
  mutations,
  getters,
  actions,
  namespaced: true
};
