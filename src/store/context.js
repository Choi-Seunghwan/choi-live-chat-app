const state = () => ({
  isMobileSize: undefined
});

const mutations = {};

const getters = {};

const actions = {
  init() {
    console.log('init');
  }
};

export default {
  namespaced: true,
  state,
  mutations,
  getters,
  actions
};
