const state = () => ({
  isMobileSize: undefined
});

const mutations = {};

const getters = {};

const actions = {
  initContext() {
    console.log('init');
  }
};

export default {
  namespaced: true,
  state,
  mutations,
  getters,
  actions
};
